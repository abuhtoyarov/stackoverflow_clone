- flash.each do |type, message|
  .alert{:class => "#{bootstrap_class_for(type.to_sym)} alert-dismissible fade in"}
    %button.close{"data-dismiss" => "alert", :type => "button"}
      %span{"aria-hidden" => "true"} ×
      %span.sr-only Close
    = message